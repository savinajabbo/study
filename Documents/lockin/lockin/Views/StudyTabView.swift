import SwiftUI

struct StudyTabView: View {
    var body: some View {
        VStack {
            Text("Begin a Study Session!")
                .font(.title)
                .padding()
            Spacer()
        }
    }
} 