import SwiftUI

struct AnalyticsView: View {
    var body: some View {
        VStack {
            Text("Analytics Dashboard Coming Soon!")
                .font(.title)
                .padding()
            Spacer()
        }
    }
} 